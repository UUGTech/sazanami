version: "2"
run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosec
    - misspell
  exclusions:
    paths:
      - "examples/"

formatters:
  enable:
    - gofmt
    - goimports

